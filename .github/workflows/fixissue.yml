name: Fix Issue

on:
  pull_request:
    branches:
      - main
jobs:
  check-compliance:
    runs-on: ubuntu-latest
    steps:
      - name: check for JIRA title
        run: [[ $github.event.pull_request.title  =~ (\\[HUDI\-[0-9]{4,}\\]|\\[MINOR\\]) ]] 
